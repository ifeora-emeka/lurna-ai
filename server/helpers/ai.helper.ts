

export class AI {
    private static readonly API_KEY = process.env.AI_API_KEY;

    static async generateText(prompt: string) {
        
    }

    static async generateStructuredData({}:{prompt: string, zodSchema: any}) {
        
    }
}
